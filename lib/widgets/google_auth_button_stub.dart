import 'package:flutter/material.dart';

Widget renderWebButton(
  BuildContext context, {
  VoidCallback? onSignInComplete,
  Function(String name)? onNameFetched,
}) {
  return const SizedBox.shrink(); // Stub for non-web platforms
}
